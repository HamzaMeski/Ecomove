package model.entities;

public class Client extends Person {
    public Client(int id, String firstName, String secondName, String phoneNumber, String email) {
        super(id, firstName, secondName, phoneNumber, email);
    }
}
